# AiCoin UI Clone

> Canonical UI: `/Users/lizekai/Documents/交易系统/aicoin-ui-clone` · `http://localhost:3002/`

独立的 AiCoin 桌面端 UI 复刻项目。包括首页、行情、快讯、要闻、策略、链上、资产、授权、数据和更多。当前版本不包含群聊与 VIP 页面或功能。

所有行情、收益率、账户、策略和交易数据均为本地 Mock；项目不会连接真实交易所、提交订单、保存 API Key 或发起支付。

当前交互范围：

- 顶部全局搜索、通知、消息、计价单位、账户与设置弹层。
- 十个一级页面及其全部顶部频道切换。
- 首页筛选、榜单、云图币块与 Ace 智搜。
- 行情自选、周期、图表工具、指标、底部工作台、AI 解读和右侧插件轨。
- 链上交易员关注与“上K线”跨页面持久化；行情图表显示可查看、切换币种和移除的交易轨迹标记。
- 行情顶部“快照 / 保存布局”与图表内部状态共用同一数据源；快照记录可查看、恢复、删除和清空。
- 行情当前交易标的为全局持久状态；顶部、K 线、深度/资金流/合约子页、盘口、技术分析、资讯和右侧本地模拟下单会同步切换，刷新后继续保持。
- 右侧本地模拟下单与底部委单区共用委托簿：限价单进入当前委托，撤单进入历史，市价成交进入买卖记录并形成可模拟平仓的现货仓位。
- 行情默认以主图和自定义指标工作台为中心，自选列表可按需展开；最后停留的一级页面与顶部频道会在刷新后恢复。
- 要闻文章选择、关注、AI 总结；策略筛选；资产组合；API 授权；列表详情检查器。
- 源码交互审计要求每个 `<button>` 都具备非空点击处理。
- 任意页面按 `/` 聚焦顶部全局搜索，按 `Esc` 清空并退出搜索。

## 启动

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

## 交互审计

```bash
npm run test:interactions
```

行情页的完整状态树和 Electron 静态逆向依据见 `docs/AICOIN_UI_REVERSE_ENGINEERING.md` 第 16—28 节。当前行情实现覆盖 9 个底部工作台、委单区 10 个子页、图表工具浮层和原版语义的右侧 9 个业务抽屉；交易相关操作停留在本地校验/预览，不提交真实订单。
