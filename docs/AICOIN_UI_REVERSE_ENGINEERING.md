# AiCoin Desktop UI 逆向拆解记录

分析对象：`/Applications/AiCoin.app`  
版本：2.16.10（build 2.16.10.21973）

## 1. 框架结论

- Electron 桌面客户端，`NSPrincipalClass = AtomApplication`。
- ARM64 Mach-O 主程序，Bundle ID 为 `com.aicoin.desktop`。
- 渲染层为 React + Webpack，主入口是 `dist/renderer/index.html`，挂载节点为 `#root`。
- 主进程入口是 `dist/main/main.js`。
- 除主窗口外，还有浏览器、OAuth、视频预览和图片预览窗口。

判定依据来自 `Info.plist`、`package.json`、`app.asar` 目录结构和 renderer bundle，不依赖界面截图。

## 2. 真实应用骨架

```text
App
├── GlobalTopBar
│   ├── 市场提示 / 运行状态
│   ├── 全局搜索
│   ├── 通知 / 消息
│   └── 计价单位 / 用户
├── PrimaryLeftRail
│   ├── 首页
│   ├── 行情
│   ├── 快讯
│   ├── 要闻
│   ├── 策略
│   ├── 链上
│   ├── 资产
│   ├── 授权
│   └── 数据 / 更多
├── PageSecondaryBar
│   └── 每个一级页面自己的频道、筛选和操作
├── MainWorkspace
│   ├── PageContextRail（策略、资产等页面出现）
│   ├── CoreContent
│   └── ContextInspector / OrderBook
├── RightUtilityRail
│   └── 行情组件、数据组件、ETF、AI Sidebar 等抽屉入口
└── BottomTicker
    └── 行情、账户状态、线路和时间
```

行情页内部继续嵌套：

```text
MarketWorkspace
├── Symbol / Layout tabs
├── ChartToolbar（指标、信号、画线、复盘、多窗、周期、显示）
├── KlineCanvas
├── RightOrderBook
├── IndicatorToolbar
└── BottomWorkbench
    ├── 委单区
    ├── 自定义指标 / 回测 / 实盘
    ├── AI 网格
    ├── DCA
    ├── 组合下单
    ├── 跟单面板
    └── AI 分析
```

## 3. 可验证的功能命名空间

从中文 locale 中恢复出的模块包括：`home`、`chart`、`indicator`、`flash`、`info`、`quant`、`trading`、`asset`、`auth`、`data`、`order`、`smartMoney`、`aiSidebar`、`aceAgent`、`alert`、`replay`、`script`、`forecastliq` 等。

首页真实频道包括 Ace 智搜、市场概览、热门榜单、排行、功能指南、云图和条件选币。行情图表真实工具包括指标、信号预警、胜率、画线、复盘、多窗、显示和周期。右侧 AI 模块包含快速问答、K 线分析、AI 写指标；Ace Agent 还包含交易授权、订单确认、拒绝、记忆和审计事件等状态。

## 4. 资源清单

| 类型       | 数量 | 用途                                     |
| ---------- | ---: | ---------------------------------------- |
| JavaScript |  224 | 主进程、React renderer、Webpack 页面分包 |
| CSS        |   88 | 全局主题和页面/组件样式                  |
| PNG        |  457 | 图标、Banner、空状态、平台与币种资源     |
| SVG        |  141 | 工具栏和功能图标                         |
| TTF        |    5 | 数字、中文、拉丁字形体系                 |
| HTML       |    5 | 主窗口、内置浏览器、OAuth、预览窗口      |
| MP4        |    5 | 教程 / 功能演示                          |
| MP3        |    7 | 快讯或预警声音                           |

字体样式中可确认 `AiCoinNumber`、`AiCoinChinese` 和 `AiCoinLatin` 三套语义字体，回退到 HarmonyOS Sans、PingFang SC、Microsoft YaHei、SF Pro 和 Segoe UI。

## 5. 原生模块与边界

`@aicoin/cryptaddon`、`@aicoin/data_server_addon`、`better-sqlite3-multiple-ciphers`、`keytar` 属于原生或敏感能力，分别涉及加密、数据服务、加密数据库和系统钥匙串。当前 UI 复刻不复制这些模块，也不接触用户密钥。

Electron 的 macOS Accessibility 暴露较浅，完整 DOM 控件关系主要以解包后的 React bundle、locale 命名空间和运行态页面核验共同确定。

## 6. 当前复刻边界

当前实现只复刻 AiCoin 自身的信息架构与交互逻辑，不再映射其他产品的研究、审批或决策流程。

- 首页：市场概览、云图、条件选币、榜单等市场发现入口。
- 行情：自选、K 线工具、指标、盘口、委单与策略工作台。
- 快讯与要闻：实时信息流、频道过滤、文章阅读和 AI 总结入口。
- 策略：自动赚币、套利、DCA、AI 网格、指标与跟单体系。
- 链上：聪明钱、巨鲸、KOL、机构与地址追踪。
- 资产与授权：多账户资产面板、盈亏/流水/风控及交易所 API 授权。
- 数据与更多：储备、Hyperliquid、钱包、宏观与自定义导航。
- 右侧工具轨：币种资料、资金、主力动向、图表、ETF、Ace Agent 和快捷交易。

群聊和 VIP 仍按用户此前明确要求排除。所有数据均为本地 Mock，不连接真实交易所，也不保存或提交 API Key。

## 7. 顶部频道路由实现

顶部二级菜单不是装饰标签，每个频道都绑定独立主内容状态：

- 首页：Ace 智搜、市场概览、条件选币、热门榜单、排行、功能指南、云图、合约雷达。
- 行情：图表、深度、资金流、合约数据、自选。
- 快讯：精选、直播、巨鲸、主力、推特、市场、特朗普、上新、链上、ETF、宏观、币股。
- 要闻：精选、活动 FUN 放、分析、直播回顾、币股、产品教程、科普、长推、媒体报道、Web3.0、行业报告、AI 号。
- 策略：自动赚币、套利机会、专业套利、我的套利。
- 资产：总览、币种分布、API 账号分布、盈亏日历、流水、风控。
- 授权：已授权账户、添加授权、安全说明。
- 数据：加密货币储备、Hyperliquid。

窄窗口中的顶部频道栏支持横向滚动，所有按钮禁止压缩，页面操作区不会覆盖靠后的频道入口。

## 8. 交互覆盖审计

- 当前 JSX 中共有 184 个按钮，184 个均绑定非空 `onClick` 处理；33 个输入框、选择器和复选框全部绑定非空状态处理；89 个动态列表根节点均具有稳定 `key`，避免频道切换和工作台重绘告警。
- 全局层：通知、消息、计价单位、账户、设置、保存、新建和帮助。
- 首页：Ace 输入/模式/问题卡、筛选器、周期、缩放、热力图币块、榜单和表格详情。
- 行情：自选切换、图表工具、周期、快照、全屏、AI 解读、指标开关和底部工作台。
- 内容与策略：文章选择、关注、AI 总结、策略分类/类型/搜索和套利配置。
- 资产与数据：资产组合、计价单位、账户过滤、小额资产、模块筛选/排序/详情。
- 右侧轨：8 个工具抽屉均可开关，快捷交易支持买卖方向切换。

项目提供 `npm run test:interactions`，通过 Babel AST 检查所有按钮和表单控件是否具有非空处理，并严格核对十个一级页面、各页面频道数量（8/5/12/12/4/3/6/3/2/4，共 59 个）和三条多步业务流程。运行态已逐一真实点击全部 59 个顶部频道入口，验证唯一选中态与主内容同步切换；同时验证自选栏、盘口、页面详情、资讯选择、资产组合、右侧八个工具抽屉和全局弹层。

## 9. 深化后的功能页面

- 快讯：重要/标题/自动刷新开关、事件时间流、利好/利空/分享、热点排行、舆情指数和趋势图。
- 链上：角色筛选、地址搜索、关注、收益/胜率/权益、详情与上K线。
- 数据：BTC/ETH 与时间周期切换、储备指标、趋势图和机构活动。
- 更多：钱包、宏观、自定义导航、帮助中心工具架。
- 行情底部：委单、自定义指标/回测/实盘、AI网格、现货DCA、组合下单、跟单和AI分析七套独立工作台。
- 右侧插件：组件管理、币种资料、资金流、主力挂单、数据图表和 ETF 六套独立面板，加 Ace Agent 与快捷交易。
- 本地持久化：自选列表、计价单位、通知/声音/自动刷新设置、套利策略、资产组合和授权账户元数据。
- 策略创建：基础信息 → 运行参数 → 确认创建，支持编辑、暂停提示和删除。
- 资产组合：组合信息 → 关联账户 → 确认，创建后写回左侧组合列表并支持删除。
- API 授权：平台与别名 → API 信息与权限 → 安全确认；只保存平台、别名、状态和日期，演示 API Key / Secret 不落盘，支持检查和撤销。

## 10. 状态型交互完善

- 行情图表：快照时间回显、全屏进入/退出、指标保存/编译/回测状态、网格收益计算和 DCA 金额校验。
- 快讯：刷新时间、利好/利空互斥反馈、分享完成态和热点榜展开/收起。
- 链上：交易员关注与“上K线/移出K线”可见状态，教程进入帮助面板。
- Ace Agent：问题输入后生成分析结果，可直接进入策略页。
- 快捷交易：价格与数量校验、本地模拟委托、买卖方向和撤单闭环，不提交真实订单。
- 右侧工具：币种自选写回、组件布局持久化、主力价格定位和 ETF 详情选择。
- 更多：自定义导航开关、保存、恢复默认以及跨刷新持久化。
- 页面操作：快照、布局和视图保存均具有明确完成态。

运行态使用全新页面依次打开十个一级页面及上述工作台，浏览器控制台应用错误为 0；测试产生的导航、布局和模拟委托状态均已恢复或清理。

## 11. 原版截图逐像素校准：行情工作台

- 以用户提供的 2048×1208 原版行情截图为硬基准，补齐“指标/周期工具条 → 绘图工具条 → K线标题与实时 OHLC → 图表 → 技术指标栏 → 快捷周期栏 → 底部工作台”的完整纵向层级。
- 新增光标、趋势线、水平线、平行线、测量、矩形、文本、斐波那契、画笔、磁吸、锁定、删除、撤销和重做等可选绘图工具，并提供可见选中态。
- K线区域增加超买/超卖信号、最高/最低点、价格轴、时间轴、实时价线和 AiCoin 水印；右侧盘口补齐成交额、净流入、最高/最低、预警/自选/策略/简况操作区。
- 套利机会表恢复价差率、持仓价值、预估回本等列，并扩充到 7 行演示数据，使信息密度接近原版。
- 自动审计现为 162 个按钮 / 162 个非空处理、77 个稳定动态列表 key；生产构建通过，1440×900 运行态控制台错误为 0。

## 12. 原版截图逐像素校准：要闻、资产与 Ace Agent

- 要闻阅读器将数字占位缩略图替换为与原版一致的横向内容缩略图体系；恢复作者/日期/热度、AI 摘要条、文章导语、正文层级和底部点赞/分享操作。
- 资产总览恢复资产趋势的 7天/30天/3月/半年切换、币种持有“更多”、下方币种分布/API账号分布/盈亏日历/流水/风控五页签，以及隐藏小额资产状态。
- Ace Agent 从通用问答抽屉升级为 390px 专属右侧工作台：Agent/Chat 模式、策略创建入口、三张策略推荐卡、换一批、胜率/小趋势/标签、策略卡回填输入和分析结果闭环。
- 页面操作区改为页面专属：要闻显示听读/阅读模式/投稿，策略显示教程，授权显示添加授权，行情保留快照/布局/新建；首页和资产不再错误显示通用“保存视图/新建”。
- 当前自动审计为 176 个按钮 / 176 个非空处理、83 个稳定动态列表 key；生产构建通过。1440×900 下逐页验证要闻、资产和 Ace Agent，资产内部页签与 Agent/Chat/策略卡回填均成功，页面控制台错误为 0。

## 13. 剩余页面逻辑补齐：授权、Hyperliquid 与更多工具

- 修复授权页“搜索可授权平台”仅有外观但不执行过滤的问题；搜索 OKX 后列表会真实收敛为一个平台，并提供无结果状态。
- 已授权账户空状态增加直接添加入口；安全说明改为四段可展开检查项，并明确当前复刻只保存平台、别名、状态与日期，API Key/Secret 不落本地存储。
- Hyperliquid 不再复用 BTC 储备页面，新增独立链上永续数据中心：市场/持仓/费率/巨鲸四页签、1H/4H/24H/7D 周期、四张指标卡、趋势图和五个市场详情入口。
- 更多页的钱包、宏观、自定义导航、帮助中心均扩充为六个工具入口；自定义导航继续支持开关、保存、恢复默认和跨刷新持久化。
- 当前自动审计为 182 个按钮 / 182 个非空处理、89 个稳定动态列表 key；生产构建通过。运行态验证 Hyperliquid 页签与周期同步、授权搜索过滤、更多页六工具布局，页面控制台错误为 0。

## 14. 全量导航与低频交互验收

- 十个一级页面全部路由到独立页面组件；59 个顶部频道逐个真实点击完成，逐项确认只有一个激活频道且主内容非空：8 + 5 + 12 + 12 + 4 + 3 + 6 + 3 + 2 + 4 = 59。
- 右侧八个工具入口逐个真实打开，标题与内容均对应：组件列表、币种资料、资金数据、主力动向、数据图表、ETF、Ace Agent、快捷交易。
- 补齐并实测低频状态：云图缩放选择、组合下单方向/数量校验与授权弹窗、画线趋势线的撤销/重做、日期定位与日期范围、资产指标切换、授权账户检查时间、Ace 附件菜单。
- 最终静态审计结果为 184 个按钮 / 184 个非空点击处理、33 个表单控件 / 33 个非空状态处理、89 个稳定动态列表 key；三条多步业务流程仍在。
- `npm run test:interactions` 与 `npm run build` 均通过；1440×900 运行态全量导航验收期间浏览器控制台应用错误为 0。

## 15. 桌面工作区连续性优化

- 移除首页、快讯、要闻、策略等顶部频道栏前重复出现的一级页面名，仅在行情页保留交易对标题，使频道起点与原版更一致。
- 行情页默认收起自选列表，把横向空间让给 K 线与盘口；图表工具栏新增自选列表展开/收起入口，用户仍可随时恢复左侧列表。
- 行情默认打开“自定义指标/回测/实盘”工作台，首屏直接呈现原版截图中的指标编辑器，不再停留在大面积委单空状态。
- 增加桌面工作区记忆：刷新后恢复最后一级页面和顶部频道；全局 `/` 快捷键聚焦搜索，`Esc` 清空并退出。
- 统一键盘焦点为低干扰内描边，并补齐 Ace、右侧工具轨、关闭与提交等纯图标按钮的可访问名称。
- 自动审计升级为 185 个按钮 / 185 个非空点击处理、33 个表单控件 / 33 个状态处理，并新增工作区持久化、行情默认面板、自选折叠和搜索快捷键的防回归契约；`npm run test:interactions`、`npm run build` 与本地 HTTP 200 检查均通过。

## 16. 行情页完整子页面逆向与实现（2026-07-22）

### 逆向依据

- 运行态：直接操作 `/Applications/AiCoin.app` 行情窗口，核对顶部双层图表工具栏、K线/指标/周期区、底部工作台、Ace Agent 和最右工具轨。
- 静态包：确认客户端为 Electron，界面资源位于 `Contents/Resources/app.asar`；解包后核对 `locales/zh/chart.json`、`trading.json`、`tradingBot.json`、`indicator.json`、`forecastliq.json`、`aiAnalysis.json` 等声明。
- 安全边界：真实下单、实盘启动、跟单和授权只实现参数、校验、预览与授权引导，不向交易所发送订单。

### 行情页状态树

- 图表工具：指标、高级、多窗、复盘、显示；周期、快照、全屏、AI解读；14 项绘图/撤销重做；主副图指标开关。
- 委单区：现货仓位、当前委托、策略、历史委托、买卖记录、账户资产、预估清算图、深度图、技术分析、主力大单。
- 自定义指标/回测/实盘：指标编辑、指标选币、策略回测、实盘运行、实盘历史。
- AI网格：创建网格、运行中、历史策略；创建态包含一键创建、手动创建、杠杆网格、收益测算和授权校验。
- 现货DCA：创建DCA、运行中、历史策略；创建态包含一键创建、手动创建、执行频率、首次执行和止盈设置。
- 组合下单：组合订单、当前委托、历史委托；支持动态增删订单、方向/类型/价格/数量与统一预览校验。
- 跟单面板：策略广场、我的跟单、跟单历史；支持风险筛选和策略详情。
- AI分析：综合解读、趋势分析、主力订单、风险事件；包含评分、数据依据、刷新和追问。
- 右侧工具轨继续提供组件列表、币种资料、资金数据、主力动向、数据图表、ETF、Ace Agent 与快捷交易八个独立抽屉。

### 实现与验收

- 新增 `src/market-workbench.jsx` 与 `src/market-workbench.css`，将行情子页面从单一占位区拆为独立状态组件。
- 图表顶部菜单不再只是选中态：指标、高级、多窗、复盘和显示均打开对应工具浮层。
- 工作台支持正常、收起和展开三种高度；横向子标签保持可滚动，不压缩后方入口。
- 交互审计扩展到新行情模块，强制核对 7 个一级工作台和 22 个关键子页面名称。
- 最终结果：233/233 个按钮具备非空处理，57/57 个表单控件具备受控或原生输入行为，120 个动态列表具有稳定 key；`npm run test:interactions` 与 `npm run build` 均通过，本地服务返回 HTTP 200。

## 17. 原版 locale 复核新增缺口（2026-07-29）

- 直接解包 `app.asar` 后，从 `locales/zh/main.json` 确认原版行情底部还包含 `bottom-tab_futures-dca = 合约DCA` 与 `bottom-tab_history = 交易历史`；此前七工作台清单并非完整集合。
- 从 `locales/zh/chart.json` 确认原版存在新增标签页、当前布局、保存布局、新建布局、我的布局、重命名与同步单窗口画线等布局命名空间。
- 从 `locales/zh/preference.json` 确认系统设置至少包含通用、K线、下单、预警、快讯弹窗、策略通知、市场异动、消息中心和 Webhook 管理，而非只有桌面通知/声音/自动刷新三个开关。
- 复刻新增图表交易对/布局标签条，支持新增、切换、关闭、保存和本地持久化；新增合约DCA的方向、杠杆、保证金、频率、加仓和止盈测算；新增交易历史的成交/策略/资金费用分类、搜索、详情和本地导出。
- 通知中心升级为分类、已读状态、详情和关联页面跳转；消息中心增加消息通知/账户活动和详情；系统设置升级为九个原版分区，并提供逐分区恢复默认、保存和 Webhook 本地管理。
- 本轮运行态已验证图表标签、合约DCA和交易历史，浏览器控制台错误为 0；设置/通知批次完成生产构建和静态交互审计，下一轮继续做独立运行态回归。
- 当前自动审计为 336/336 按钮、116/116 表单控件和 159 个稳定动态列表 key；生产构建通过。安全边界仍为无群聊、无 VIP、无真实下单和无密钥持久化。

## 18. 预警中心与全局入口闭环（2026-07-29）

- 依据原版 `locales/zh/alert.json` 补齐独立预警中心，包含“我的预警 / 历史预警”、价格预警、指标预警、价差预警、资金费率和链上预警五种类型。
- 行情右侧盘口“加预警”不再停留在单个价格输入框，改为进入统一预警中心；图表“高级 → 信号预警”会直接打开已选择“指标预警”的新增表单。
- 新增交易对、周期、触发条件、阈值、频率、PC 通知、Webhook、备注等配置；支持新增、编辑、暂停/恢复、删除归档、类型筛选、交易对搜索和历史记录重新添加。
- 预警与历史记录分别写入 `aicoin-alerts` 和 `aicoin-alert-history` 本地存储；编辑暂停中的预警会保留暂停状态，不会隐式恢复运行。
- 浏览器实际完成新增指标预警、Webhook 选择、暂停、编辑、删除归档、历史恢复和信号预警直达测试；系统设置、通知中心和消息中心也完成运行态回归，控制台应用错误为 0。
- 当前自动审计为 350/350 按钮、123/123 表单控件和 167 个稳定动态列表 key；`npm run test:interactions` 与 `npm run build` 均通过。生产包仍只有 Vite 的单包体积提示，不影响运行。

## 19. 行情策略生命周期闭环（2026-07-29）

- 继续复核原版 `locales/zh/tradingBot.json`、`copyTrade.json`、`indicator.json`、`order.json` 与 `trading.json`，确认网格、DCA、指标实盘和跟单都不是一次性创建页，而是“创建 → 运行/暂停 → 停止归档 → 历史重启”的持续状态系统。
- AI 网格补齐价格区间、网格数量、投入金额校验、收益测算、创建本地模拟策略、运行/暂停/恢复、详情、终止归档和历史重新运行。
- 现货 DCA 补齐金额、频率、首次执行、止盈参数；合约 DCA 补齐方向、杠杆、保证金、加仓与止盈测算。两者均接入同一套运行中/历史策略生命周期。
- 指标回测结果新增“创建模拟实盘”，与侧栏实时数量、实盘运行、暂停/恢复、终止、实盘历史和重新运行连通。
- 跟单面板补齐策略详情、跟单模式、投入上限、跟单止损、我的跟单、暂停/恢复、结束归档、跟单历史与重新跟单配置；全部仅写本地模拟状态，不绑定账户或提交真实委托。
- 五类状态分别持久化到 `aicoin-grid-strategies`、`aicoin-spot-dca-strategies`、`aicoin-futures-dca-strategies`、`aicoin-indicator-live-strategies` 与 `aicoin-copy-subscriptions`。
- 浏览器实际跑通网格、现货 DCA、合约 DCA、指标实盘和跟单完整链路；跟单参数 650 USDT / 75 USDT 经创建、暂停、恢复、归档和重新配置后保持一致，控制台应用错误为 0。
- 当前自动审计为 361/361 按钮、126/126 表单控件和 171 个稳定动态列表 key；`npm run test:interactions` 与 `npm run build` 均通过。生产包仅保留 Vite 的单包体积提示。

## 20. K线真实绘图与快捷模拟委托（2026-07-29）

- 修正绘图工具“只有选中提示、没有图表对象”的问题：趋势线、水平线、平行线、测量、矩形、文本、斐波那契和画笔现在会按点击位置真实渲染到 K 线绘图区。
- 磁吸会把落点吸附到 5% 网格；锁定会阻止新增和删除；删除会清空当前对象；撤销/重做保存的是整份绘图快照，不再只是切换工具名称。
- 绘图对象写入 `aicoin-chart-drawings`，刷新页面后继续显示；图表语义树同步暴露对象数量，便于运行态回归。
- 右侧快捷交易新增限价/市价、25/50/75/100% 数量快捷值、预估金额、委托时间和等待成交/模拟成交状态。
- 模拟委托写入 `aicoin-quick-orders`，关闭并重开右侧面板后仍保留；支持单笔撤单/删除和全部清空，始终不提交真实交易。
- 浏览器实际完成趋势线、磁吸矩形、锁定拦截、删除、撤销、重做，以及限价/市价模拟单、跨面板持久化和清空测试；应用错误为 0。
- 当前自动审计为 364/364 按钮、126/126 表单控件和 174 个稳定动态列表 key；`npm run test:interactions` 与 `npm run build` 均通过，仅有 Vite 单包体积提示。

## 21. 策略顶部导航、套利筛选与收藏闭环（2026-07-29）

- 继续核对原版 `locales/zh/quant.json`，确认策略页顶部频道为“自动赚币 / 套利机会 / 专业套利 / 我的套利”，套利机会内部另有方向、期限、跨所和收藏维度。
- 修正顶部频道只改变选中样式、不驱动左侧业务分类的问题：自动赚币会进入全币种 DCA，套利机会与专业套利进入智能套利，我的套利进入本地策略管理；每次切换同时重置对应筛选条件。
- 套利表新增平台与合约类型筛选，并让正向、反向、跨所永续、永续-期货、价差-期现和收藏六类子频道产生真实不同的数据集合。
- 每个套利组合新增本地收藏/取消收藏，写入 `aicoin-arb-favorites`；顶部频道来回切换后收藏仍保留，收藏列表为空时提供清除筛选入口。
- 修正“我的套利”状态锁死左侧导航的问题：点击 AI 网格、DCA、指标或跟单分类会退出套利子导航，显示该分类自己的标题、说明、风险筛选与策略卡片。
- 浏览器实测币安筛选为 4 条、期货筛选为 2 条；收藏跨频道保持为 1 条；从我的套利进入 AI 网格后不再残留套利筛选栏，运行时应用错误为 0。
- 当前自动审计为 366/366 按钮、128/128 表单控件和 174 个稳定动态列表 key；`npm run test:interactions` 与 `npm run build` 均通过，仅保留 Vite 单包体积提示。

## 22. 自动赚币与专业套利独立工作台（2026-07-29）

- 进一步核对原版 `locales/zh/quant.json` 后纠正导航语义：顶部“自动赚币 / 套利机会 / 专业套利 / 我的套利”都属于左侧“智能套利”，不应把自动赚币错误跳到 DCA、把我的套利错误跳到策略广场。
- 自动赚币新增“精选 / 仅看自选 / 按收益率”、币种搜索、平台筛选、组合收藏、收益指标和开始赚币入口；收藏复用 `aicoin-arb-favorites`，创建仍进入三步本地策略向导。
- 自动赚币创建向导现在能保留 PENDLE 等来源币种和“自动赚币”类型，不再因固定下拉选项静默回退到 BTC/正向套利。
- 专业套利新增套利组合侧栏、左右腿币对与方向配置、组合新增/删除、价差 K 线、开平仓价差、资金费差、双边市价/挂单/超价、同金额/同数量、金额校验、双腿预览和本地模拟记录。
- 套利组合与模拟订单分别写入 `aicoin-arb-combinations`、`aicoin-professional-arb-orders`；模拟订单跨顶部频道切换保持，支持单条删除和全部清空，永不向交易所发单。
- 修复所有一级页面/顶部频道切换时内容区不回顶部的问题，避免标题被上一页面滚动位置裁切。
- 浏览器实测币安自动赚币筛选为 3 条、收藏筛选为 1 条；成功创建并恢复 ETH 双腿套利组合，250 USDT × 2 的双边超价预览和模拟记录参数一致；测试数据已清理，运行时应用错误为 0。
- 当前自动审计为 384/384 按钮、136/136 表单控件和 181 个稳定动态列表 key；`npm run test:interactions` 与 `npm run build` 均通过，仅保留 Vite 单包体积提示。

## 23. 策略左侧产品与行情生命周期统一（2026-07-29）

- 策略页左侧“全币种 DCA / AI网格 / 指标策略 / 跟单策略·CEX / 跟单策略·DEX”不再停留在静态模板卡，统一复用行情页已经验证的创建、运行、暂停、终止、历史与重启状态组件。
- 新增 `StrategyProductWorkbench` 复用层，并在策略页提供统一交易标的选择、授权状态提示及完整产品工作台；DCA、网格和指标实盘继续使用原有 localStorage 数据，因此从行情页或策略页进入看到的是同一份状态。
- `BotStrategyList` 增加“清空历史”，让网格、DCA、指标实盘等本地模拟生命周期具备可逆清理出口。
- CEX 与 DEX 跟单现在使用不同数据源、策略卡和持久化键：CEX 为 `aicoin-copy-subscriptions`，Hyperliquid DEX 为 `aicoin-dex-copy-subscriptions`；DEX 展示链上地址、PERP 标的与独立风险文案。
- 修复 `useLocalState` 在存储键变化时不重新载入的问题，解决 CEX 切换到 DEX 后错误沿用 CEX 订阅的状态串线；跟单历史也新增独立清空入口。
- 策略分类状态提升到应用层：左侧选择非智能套利产品时，顶部不再错误高亮上一次“专业套利”；重新进入智能套利时恢复“自动赚币 / 套利机会 / 专业套利 / 我的套利”四频道。
- 浏览器真实创建并清理 ETH 120 USDT DCA、SOL 80 USDT 网格、SOL 指标模拟实盘；CEX 610 USDT 与 DEX 720 USDT 跟单并行存在且互不串线，随后均终止归档并清空。运行时应用错误为 0。
- 当前自动审计为 386/386 按钮、137/137 表单控件和 181 个稳定动态列表 key；`npm run test:interactions` 与 `npm run build` 均通过，仅保留 Vite 单包体积提示。

## 24. 行情右侧九业务子页按原版语义重构（2026-07-29）

- 运行态再次打开 `/Applications/AiCoin.app` 行情窗口，交叉核对 `locales/zh/main.json` 的 `sideTab_*` 声明，确认右侧业务页应为 Ace Agent、AI助手、技术分析、订单表与最新成交、ETF数据、特色数据、资金流向、最新资讯和下单面板；组件列表属于辅助入口，不应占用业务子页名额。
- 右侧工具轨按上述九类重新排列并使用原版名称；组件列表和帮助移动到独立的底部辅助区。旧的“币种资料 / 主力动向 / 数据图表”等近似命名不再冒充原版业务页。
- AI助手支持行情问答、指标帮助、功能导航、快捷问题回填和回答结果；技术分析支持 5分/15分/1时/4时评分、分项信号和支撑阻力；订单表与最新成交支持盘口/成交切换和价格选择反馈。
- 特色数据支持主力挂单、合约、链上三类独立数据；最新资讯支持快讯、要闻、自选相关三类列表与详情预览；ETF、资金流、Ace Agent 和下单面板继续保留各自独立状态与业务动作。
- 浏览器逐个打开九个业务抽屉，并实际验证 AI 问答、4小时技术评分、最新成交选价、链上特色数据、资讯详情和本地模拟委托创建/撤销，全部产生可观察结果；测试委托已撤销。
- 当前自动审计为 403/403 按钮、138/138 表单控件和 196 个稳定动态列表 key；`npm run test:interactions` 与 `npm run build` 均通过，仅保留 Vite 单包体积提示。

## 25. 链上交易员“上K线”跨页面闭环（2026-07-29）

- 修复链上卡片的“关注 / 上K线”只存在于 `ChainPage` 临时状态、离开页面后失效且行情图表完全不消费该状态的问题。
- 关注列表和 K 线交易员分别持久化到 `aicoin-followed-traders` 与 `aicoin-chart-traders`；市场、角色、币种、方向和参考价随交易员记录保存。
- 链上英雄区会显示已上 K 线数量并可直接进入行情；行情图表增加交易员管理条，可切换到对应币种、逐个移除或全部清空。
- 对应币种 K 线会渲染买入/卖出交易员标记；点击标记打开交易员详情，不触发当前绘图工具，也不会生成真实交易。
- 浏览器实际完成“关注 → 上K线 → 进入 BTC 图表 → 打开标记详情 → 刷新保持 → 行情移除 → 链上同步 → 清理关注”完整链路，所有测试状态已清理。
- 当前自动审计为 408/408 按钮、138/138 表单控件和 198 个稳定动态列表 key；新增跨页持久化防回归契约，`npm run test:interactions` 与 `npm run build` 均通过。

## 26. 行情顶部快照与布局操作接入真实图表状态（2026-07-29）

- 修复页面顶部“快照 / 保存布局”和图表内部状态彼此独立的问题：顶部按钮此前只改变自身文字，不保存当前币种、周期、指标、绘图或活动布局。
- 新增 App 级行情命令通道；顶部快照与图表相机现在统一生成包含币种、周期、指标、绘图数、布局名称、来源和时间的记录，持久化到 `aicoin-chart-snapshots`，最多保留 20 条。
- 图表布局栏新增快照记录入口；独立管理器支持查看、应用到图表、单条删除和全部清空。恢复快照会同步恢复币种、周期、指标与仍存在的布局标签。
- 顶部“保存布局”调用图表真实布局保存函数；币种切换导致的未保存圆点会在保存后恢复为“默认/已保存”状态。
- 浏览器保存了 5分与 15分+RSI 两份不同快照，恢复 5分快照后周期和指标同步回滚；切换 ETH 使布局变脏，顶部保存后脏状态消失。测试快照已清理并恢复 BTC。
- 当前自动审计为 413/413 按钮、138/138 表单控件、199 个稳定动态列表 key 和 32 种专属弹层；`npm run test:interactions` 与 `npm run build` 均通过。

## 27. 行情交易标的全局一致性（2026-07-29）

- 修复“切币只改变 K 线标题”的状态断裂：原实现把 `symbol` 留在图表页局部，页面顶部、深度频道、盘口、右侧分析与快捷下单仍写死 BTC，切换顶部子页后也无法继承当前标的。
- 新增 `aicoin-market-symbol` 应用级持久状态和统一行情模型；BTC、ETH、SOL、HYPE、XRP、ADA、AVAX 分别提供价格、涨跌、开高低、成交额、净流入、市值、波动率和价格精度。
- 顶部交易对、图表标签与 OHLC、价格轴、深度中间价、盘口卖买档、成交记录、预警、策略入口、技术支撑阻力、币种资料、资讯、主力挂单和右侧下单面板均消费同一标的与行情快照。
- 本地模拟委托记录下单时的币种，切换标的后历史记录不会被错误显示成当前币种；价格预估也使用当前标的市场价。所有操作仍仅为本地模拟。
- 浏览器真实完成 BTC→ETH、图表→深度、ETH 市价模拟下单和刷新保持回归；验证顶部、深度标题、中间价、盘口与下单单位一致。测试订单已删除，当前标的恢复 BTC。
- 当前自动审计为 413/413 按钮、138/138 表单控件和 200 个稳定动态列表 key；新增全局标的一致性契约，`npm run test:interactions` 与 `npm run build` 均通过，仅保留 Vite 单包体积提示。

## 28. 右侧下单与底部委单区共享本地委托簿（2026-07-29）

- 修复右侧下单面板与底部委单区完全断开的伪功能：此前创建模拟订单后，“现货仓位(0) / 当前委托(0) / 历史委托 / 买卖记录”始终展示空表。
- `aicoin-quick-orders` 成为两处共同数据源，并通过页面内状态广播实现即时同步；币种筛选可限制为当前交易对。
- 限价模拟单进入当前委托；从底部或右侧撤单都会写入“已撤销”状态并进入历史委托。市价模拟单进入买卖记录，并按成交方向和数量聚合成本、现价与盈亏形成现货仓位。
- 仓位支持本地模拟平仓：生成反向市价成交记录并把净仓位归零。每个子页的数量角标会随委托和仓位实时变化。
- 底部状态栏同时接入当前全局交易标的，切换 ETH 后展示 ETH/USDT、对应涨跌与价格，不再固定 BTC。
- 浏览器真实完成“限价下单 → 当前委托 → 撤单 → 历史委托 → 市价成交 → 买卖记录 → 现货仓位 → 模拟平仓”完整链路；另验证 ETH 底部行情联动。测试订单全部清理并恢复 BTC。
- 当前自动审计为 415/415 按钮、138/138 表单控件和 204 个稳定动态列表 key；新增共享委托簿防回归契约，`npm run test:interactions` 与 `npm run build` 均通过。

## 29. 参考 AiCoin_cleaned_v5 补全 K 线复盘（2026-08-02）

- 对照 `AiCoin_cleaned_v5/renderer/9256/deobfuscated.js` 验证复盘业务语义，确认原复刻版只有“开始K线复盘”菜单入口，缺少实际训练层。
- 新增复盘进度、播放/暂停、单步、1x/2x/4x 倍速、选择起始时间、重新选择起点和简洁/持仓模式切换。
- 新增本地模拟买入/卖出、持仓、浮动盈亏和交易次数反馈；结束训练会显示进度、盈亏、交易次数与推荐指标胜率摘要。
- 所有复盘委托只存在于组件内存，不写入真实委托簿、不连接交易所，并在结果页明确标识不会启用实盘交易。
- 浏览器在 `http://localhost:3002/` 实测“启动复盘 → 单步 → 模拟买入 → 播放/暂停 → 结束训练”，训练进度 21%、模拟持仓 0.01 BTC、盈亏反馈和结果页均正常。
- 当前自动审计为 427/427 按钮、138/138 表单控件和 206 个稳定动态列表 key；新增 K 线复盘防回归契约，`npm run test:interactions` 与 `npm run build` 均通过。

## 30. 推荐缺口统一补全（2026-08-02）

- 自定义指标工作台新增 AI 指标助手，支持自然语言需求、快捷提示、生成中状态、代码结果、写入编辑器和直接进入回测；生成结果明确限定为本地研究。
- AI K 线解读新增画线、创建预警、模拟下单和一键分享四个结果动作，每项都有独立完成状态与撤销反馈。
- 网格、现货 DCA、合约 DCA 和指标策略共用的生命周期列表统一升级：多选、全选、批量暂停/恢复/终止，以及收益概览、当前委托、成交记录、策略参数四类订单详情。
- 策略参数可本地修改；新增行情连接中断异常横幅、状态筛选、重试与检查恢复链路，覆盖原版运行策略错误态。
- 当前自动审计为 442/442 按钮、142/142 表单控件和 213 个稳定动态列表 key；推荐补全项均加入防回归契约，`npm run test:interactions` 与 `npm run build` 通过。

## 31. 行情图表升级为 TradingView Lightweight Charts（2026-08-02）

- 依据用户提供的原版行情截图，重新确认行情工作台的核心不是静态蜡烛图，而是市场分类、交易平台、可交互多窗 K 线、盘口与底部策略区共同组成的专业桌面工作区。
- 接入 TradingView 官方开源 `lightweight-charts` 5.2；许可证为 Apache-2.0，并保留图表内 TradingView attribution 标识。
- K 线支持鼠标滚轮缩放、拖拽、触控缩放、十字光标、价格轴、时间轴、MA7/MA25 与成交量；切换周期或标的会重建对应数据视图。
- “多窗”菜单的单窗、双窗和四窗现在驱动真实图表网格；副窗自动使用 4时、1日和15分周期。
- 原复刻的本地画线、磁吸/锁定状态与链上交易员标记叠加在新图表层上，避免图表内核升级破坏既有跨页面闭环。
- 左侧行情新增自选、平台、指数、美股合约、板块分类，以及币安、欧易 OKX、Hyperliquid、Bitget 平台切换；加入黄金、白银与美股合约演示行情。
- 自动审计新增 TradingView 图表契约并通过；生产构建通过。浏览器自动接管因本机 URL 安全策略被阻止，未绕过限制，保留 3002 页面供人工拖拽和缩放验收。

## 32. 行情页潜在控件全量功能化（2026-08-02）

- 对照 `AiCoin_cleaned_v5` 中的图表菜单、高级行情、复盘、多窗和工具条语义，将原有只切换样式或弹提示的入口改成图表、面板或业务状态变化。
- 指标与显示项会增删 Lightweight Charts 序列、价格线、网格、信号标记和指标读数；筹码、大额成交、主力大单、挂单统计与胜率均有独立可见结果。
- 多窗扩展到单、双、四、六、九窗；布局管理器可命名、预览和保存布局；图表分享与新标签产生独立结果。
- 根据用户原版截图红框复核，新增蜡烛、空心蜡烛、折线和面积图类型切换；每个多窗 K 线面板拥有独立周期选择器，不再将副窗周期写死。
- 画线工具覆盖趋势线、水平线、平行线、测量、矩形、文本、斐波那契与画笔，保留磁吸、锁定、撤销、重做、删除和本地持久化。
- 复盘日期弹层通过回调写回起点；隐藏未来 K 线会实际截断图表数据，不再只更改文案。倍速、单步、播放、模拟买卖和训练摘要保持联动。
- 平台切换同步作用于图表标题、本地演示报价和委托簿；市场列表分类、搜索、排序和标的切换可组合使用。
- 自动审计覆盖截图标注的关键行情控件；460/460 按钮、145/145 表单控件、228 个动态列表 key 和生产构建均通过。

## 33. 优先保障 UI 完整的 3001 能力迁移（2026-08-05）

- 保留 3002 完整 AiCoin 页面、Phosphor 图标与 Lightweight Charts 内核，只吸收 3001 中能直接补强现有 UI 闭环的行为。
- 图表绘图层新增原生右键菜单；菜单按光标纵向位置换算参考价，提供预警、买卖限价回填、自选、复盘、重置和清图六类结果。
- 盘口与最新成交行改为可点击价格源，通过 `aicoin-prefill-order` 事件打开右侧下单抽屉，回填标的、买卖方向与限价。
- 系统设置增加盘口深度背景条开关；红涨绿跌与绿涨红跌偏好进入全局 CSS 变量及图表序列配色。
- EMA 采用指数移动平均，BOLL 使用 20 期均线与两倍标准差，RSI、MACD/信号线与 KDJ 读数基于当前图表数据动态计算。
- 防回归契约覆盖右键菜单、点价事件、深度条、配色偏好和指标函数；472/472 按钮、145/145 表单控件、生产构建和 3002 HTTP 200 通过。
